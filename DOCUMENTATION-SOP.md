# 文档编写标准操作流程 (SOP)

> **版本**：v1.1  
> **最后更新**：2026-04-21  
> **维护者**：Aone Copilot

## 📋 概述

本文档总结了在撰写 Effect 学习路线图文档过程中积累的最佳实践和标准操作流程，用于指导后续文档编写工作。

## 🎯 核心原则

### 1. 质量优先

- **严禁未实现的部分**：所有代码示例必须完整可运行
- **严禁遗留 TODO**：不得留下待完成的标记
- **严禁简化实现**：所有功能必须提供完整实现
- **严禁假设实现**：所有功能必须有完整代码

### 2. 一致性

- 统一的文档结构和格式
- 统一的命名规范（kebab-case）
- 统一的代码风格（JavaScript）
- 统一的注释规范

### 3. 实用性

- 提供完整可运行的代码示例
- 包含实际应用场景
- 提供学习检查点
- 给出下一步学习路径

## 📝 文档结构规范

### 标准文档模板

```markdown
# 文档标题

> **版本**：v1.0  
> **最后更新**：YYYY-MM-DD

## 📚 概念介绍

简要介绍本节要讲解的核心概念

## 🎯 主题一

### 子主题一

\```js
// 完整的代码示例
\```

### 子主题二

\```js
// 完整的代码示例
\```

## 💡 主题二

### 子主题一

\```js
// 完整的代码示例
\```

## 📝 实际应用示例

### 综合示例

\```js
// 完整的综合应用示例
\```

## 🔗 相关概念

- **相关概念1**：简要说明
- **相关概念2**：简要说明
- **相关概念3**：简要说明

## 📚 学习检查点

- [ ] 学习点1
- [ ] 学习点2
- [ ] 学习点3

## 🚀 下一步

→ [下一文档标题](./下一文档路径.mdx)
```

### 必需元素

1. **版本信息**：每个文档必须有版本号和更新日期
2. **概念介绍**：简要说明本节要讲解的内容
3. **代码示例**：所有代码必须完整可运行
4. **实际应用**：提供综合应用示例
5. **相关概念**：列出相关的学习内容
6. **学习检查点**：帮助用户检验学习效果
7. **下一步**：引导用户继续学习

## 💻 代码编写规范

### 代码格式

- 使用 JavaScript（除非明确要求 TypeScript）
- 所有代码示例必须完整可运行
- 使用 `import { Effect } from 'effect'` 导入
- 使用 `Effect.runSync()` 或 `Effect.runPromise()` 执行

### 代码示例模板

```js
import { Effect } from 'effect'

// 简要说明这段代码的作用
const program = Effect.gen(function* () {
  // 完整的实现逻辑
  const data = yield* fetchData()
  const processed = yield* processData(data)
  return processed
})

Effect.runSync(program)
```

### 代码注释

- 关键逻辑必须添加注释
- 使用 `//` 进行单行注释
- 注释要简洁明了，说明"为什么"而非"是什么"

## 🔍 质量检查清单

### 文件创建前

- [ ] 确认文件名使用 kebab-case
- [ ] 确认文件扩展名为 `.mdx`
- [ ] 确认文件路径符合项目结构

### 文件创建后

- [ ] 检查是否有未实现的部分
- [ ] 检查是否有遗留的 TODO
- [ ] 检查是否有简化实现
- [ ] 检查是否有假设实现
- [ ] 检查所有代码示例是否可运行
- [ ] 检查是否遵循项目规范

### 文件编辑前

- [ ] 使用 `read_file` 读取文件内容
- [ ] 确认要修改的内容
- [ ] 避免修改原有内容

### 文件编辑后

- [ ] 检查修改是否正确
- [ ] 检查是否引入新的问题
- [ ] 检查格式是否一致
- [ ] **确认版本号已更新**
- [ ] **确认更新日期已更新**

## 📂 项目结构规范

### 目录结构

```
docs/effect/
├── 01-basics/           # 基础概念
├── 02-core-concepts/    # 核心概念
├── 03-ecosystem/        # 生态系统
├── 04-practical-applications/  # 实践应用
├── 05-advanced-patterns/       # 高级模式
├── 06-ai-integration/          # AI 集成
├── 07-best-practices/          # 最佳实践
│   ├── 01-architecture-design/
│   ├── 02-code-quality/
│   └── 03-development-workflow/
└── _meta.json          # 元数据配置
```

### 文件命名

- 使用 kebab-case（小写字母、连字符）
- 示例：`01-code-style.mdx`、`02-testing-strategies.mdx`

### 元数据配置

#### 章节级 _meta.json（用于包含子目录的章节）

每个包含子目录的章节必须创建 `_meta.json` 文件：

```json
[
  {
    "type": "dir",
    "name": "01-subsection",
    "label": "1.1 子章节标题",
    "collapsed": true
  },
  {
    "type": "dir",
    "name": "02-subsection",
    "label": "1.2 子章节标题",
    "collapsed": true
  }
]
```

#### 文档级 _meta.json（用于包含文档的目录）

每个包含文档的目录必须创建 `_meta.json` 文件：

```json
[
  "01-document1",
  "02-document2",
  "03-document3"
]
```

**注意**：
- 章节级使用对象数组，包含 `type`、`name`、`label`、`collapsed` 字段
- 文档级使用字符串数组，只包含文件名（不含扩展名）
- 文件名使用 kebab-case，不带 `.mdx` 扩展名

## 🔄 工作流程

### 新文档创建流程

1. **需求分析**
   - 确定文档主题和范围
   - 列出要讲解的核心概念
   - 规划文档结构

2. **内容撰写**
   - 按照标准模板撰写文档
   - 确保所有代码示例完整可运行
   - 提供实际应用示例

3. **质量检查**
   - 检查是否有未实现的部分
   - 检查是否有遗留的 TODO
   - 检查所有代码示例

4. **文件创建**
   - 使用 `create_file` 工具创建文件
   - 确保文件名使用 kebab-case
   - 确保文件扩展名为 `.mdx`

5. **元数据更新**
   - 更新 `_meta.json` 文件
   - 添加新文档的路由信息

6. **最终验证**
   - 读取创建的文件
   - 确认内容完整无误
   - 确认格式正确

### 文档编辑流程

1. **读取文件**
   - 使用 `read_file` 读取文件内容
   - 确认要修改的部分
   - 记录当前版本号

2. **修改内容**
   - 使用 `file_replace` 或 `edit_file` 修改
   - 确认修改正确
   - **修订版本号**：每次编辑后必须更新版本号（如 v1.0 → v1.1）
   - **更新日期**：更新"最后更新"日期为当前日期

3. **验证修改**
   - 再次读取文件确认修改
   - 检查是否引入新问题
   - 确认版本号已更新

## ⚠️ 常见错误及预防

### 错误1：代码示例不完整

**症状**：代码示例缺少导入、缺少关键逻辑

**预防**：
- 所有代码示例必须包含完整的导入语句
- 所有代码示例必须包含完整的执行逻辑
- 确保代码可以直接复制运行

### 错误2：遗留 TODO

**症状**：文档中包含 `TODO:` 或 `FIXME:` 标记

**预防**：
- 撰写时完成所有内容
- 不要留下待完成的标记
- 如需后续补充，使用明确的占位符说明

### 错误3：简化实现

**症状**：使用 `// ... 实现细节省略` 等注释

**预防**：
- 提供完整的实现
- 如果代码过长，可以拆分多个示例
- 确保每个示例都是完整的

### 错误4：假设实现

**症状**：代码中引用未定义的函数或变量

**预防**：
- 所有引用的函数必须定义
- 所有引用的变量必须声明
- 确保代码可以独立运行

## 📊 任务管理规范

### 使用 Todo List

对于复杂任务（3个或以上步骤），必须使用 `todo_write` 管理任务进度：

```javascript
{
  "merge": false,
  "todos": [
    {
      "id": "1",
      "content": "任务1描述",
      "status": "in_progress"
    },
    {
      "id": "2",
      "content": "任务2描述",
      "status": "pending"
    }
  ],
  "note": "关键信息记录"
}
```

### 任务状态

- `pending`：尚未开始
- `in_progress`：正在进行
- `completed`：已完成
- `cancelled`：已取消

### 更新规则

- 开始新任务时，将当前任务标记为 `in_progress`
- 完成任务后，标记为 `completed`
- 一次只能有一个任务处于 `in_progress` 状态

## 🔧 工具使用规范

### read_file

- 读取文件前必须先确认文件路径
- 对于大文件，使用行号范围读取
- 读取后检查内容是否完整

### create_file

- 创建前确认文件不存在
- 确保文件名使用 kebab-case
- 确保文件扩展名为 `.mdx`

### edit_file / file_replace

- 编辑前必须先读取文件
- 使用 `file_replace` 进行精确替换
- 使用 `edit_file` 进行复杂编辑

### todo_write

- 复杂任务必须使用
- 及时更新任务状态
- 记录关键信息到 note

## 📚 学习资源

### 参考文档

- 项目规范：`AGENTS.md`
- Rspress 文档：https://rspress.rs/llms.txt
- Effect 官方文档：https://effect.website/

### 示例参考

参考 `docs/ai/01-llm-fundamentals/01-llm-concepts` 目录的文档格式

## ✅ 完成标准

文档完成的标准：

1. ✅ 所有代码示例完整可运行
2. ✅ 没有未实现的部分
3. ✅ 没有遗留的 TODO
4. ✅ 没有简化实现
5. ✅ 没有假设实现
6. ✅ 遵循项目规范
7. ✅ 通过质量检查

## 🚀 最佳实践总结

### 撰写技巧

1. **从简单到复杂**：先讲解基本概念，再介绍高级应用
2. **提供完整示例**：每个概念都要有完整的代码示例
3. **实际应用场景**：提供真实场景的应用示例
4. **循序渐进**：按照学习路径组织内容

### 代码示例

1. **完整性**：所有代码必须完整可运行
2. **可读性**：使用清晰的变量名和注释
3. **实用性**：提供有实际价值的示例
4. **一致性**：使用统一的代码风格

### 质量保证

1. **自检**：完成质量检查清单
2. **验证**：确保代码可以运行
3. **审查**：检查是否符合规范
4. **测试**：验证文档的正确性

## 📝 版本历史

- **v1.1** (2026-04-21)：修正元数据配置格式，增加版本号更新规范
- **v1.0** (2026-04-21)：初始版本，基于 Effect 学习路线图文档编写经验总结

---

**注意**：本 SOP 会根据实际使用情况持续更新和完善。
