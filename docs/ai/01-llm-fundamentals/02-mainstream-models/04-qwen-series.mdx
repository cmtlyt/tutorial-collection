# Qwen 系列模型详解

> **版本**：v1.0  
> **最后更新**：2026-04-14

## 📖 概述

Qwen（通义千问）是阿里巴巴达摩院开发的大语言模型系列。Qwen 系列以其强大的中文能力、优秀的多语言支持和丰富的模型生态，成为中文领域最受欢迎的开源 LLM 之一。

## 🏆 模型系列

### Qwen-1（2023年8月）

**基本信息**
- 发布时间：2023年8月
- 参数规模：1.8B, 7B, 14B, 72B
- 上下文长度：2K-8K tokens
- 训练数据：2.2T tokens
- 许可证：开放许可

**特点**
- 中文能力突出
- 多语言支持
- 代码能力强
- 开源友好

### Qwen-1.5（2024年2月）

**基本信息**
- 发布时间：2024年2月
- 参数规模：0.5B, 1.8B, 4B, 7B, 14B, 32B, 72B
- 上下文长度：32K tokens
- 训练数据：3T tokens
- 许可证：Apache 2.0

**改进点**
- 上下文长度扩展到 32K
- 性能大幅提升
- 多模态支持
- 更好的对齐

**性能表现**
- 在多项基准测试中超越 LLaMA 2
- 中文能力领先
- 代码能力优异

### Qwen2（2024年6月）

**基本信息**
- 发布时间：2024年6月
- 参数规模：0.5B, 1.5B, 4B, 7B, 72B
- 上下文长度：32K-128K tokens
- 训练数据：12T tokens
- 许可证：Apache 2.0

**重大更新**
- 使用 12T tokens 训练数据
- 性能接近 GPT-4
- 支持超长上下文（128K）
- 多语言能力增强

### Qwen2.5（2024年9月）

**基本信息**
- 发布时间：2024年9月
- 参数规模：0.5B, 1.5B, 3B, 7B, 14B, 32B, 72B
- 上下文长度：32K-128K tokens
- 训练数据：18T tokens
- 许可证：Apache 2.0

**革命性更新**
- 推出 0.5B-72B 完整系列
- 性能达到顶级水平
- 代码能力大幅提升
- 数学推理能力增强

### Qwen2.5-Coder

**基本信息**
- 发布时间：2024年9月
- 参数规模：0.5B, 1.5B, 3B, 7B, 14B, 32B
- 上下文长度：32K tokens
- 专门优化：代码生成

**特点**
- 专门的代码模型
- 支持 92 种编程语言
- 代码补全能力强
- 代码理解能力优异

### Qwen2-VL

**基本信息**
- 发布时间：2024年8月
- 参数规模：2B, 7B, 72B
- 模态：文本 + 图像 + 视频
- 上下文长度：128K tokens

**特点**
- 多模态理解
- 图像描述
- 视频理解
- 视觉问答

## 🏗️ 模型架构

### 核心架构特点

Qwen 基于 Transformer 架构，但做了多项优化：

1. **SwiGLU 激活函数**
   - 替代传统的 ReLU
   - 性能更好
   - 训练更稳定

2. **旋转位置编码（RoPE）**
   - 更好的位置表示
   - 支持外推
   - 计算高效

3. **查询键值分离（GQA）**
   - 减少内存占用
   - 提高推理速度
   - Qwen2.5 引入

4. **滑动窗口注意力**
   - 支持长上下文
   - 计算效率高
   - Qwen2 引入

### 架构对比

| 特性 | Qwen-1 | Qwen-1.5 | Qwen2 | Qwen2.5 |
|------|--------|----------|-------|---------|
| 归一化 | RMSNorm | RMSNorm | RMSNorm | RMSNorm |
| 激活函数 | SwiGLU | SwiGLU | SwiGLU | SwiGLU |
| 位置编码 | RoPE | RoPE | RoPE | RoPE |
| 注意力 | MHA | MHA | GQA | GQA |
| 上下文 | 2-8K | 32K | 32-128K | 32-128K |

## 📊 性能对比

### 基准测试结果

| 模型 | MMLU | HumanEval | GSM8K | C-Eval |
|------|------|-----------|-------|---------|
| Qwen-1 72B | 70.2% | 35.5% | 61.2% | 82.3% |
| Qwen-1.5 72B | 78.9% | 48.2% | 75.3% | 88.5% |
| Qwen2 72B | 82.6% | 52.1% | 82.7% | 90.2% |
| Qwen2.5 72B | 86.5% | 59.3% | 89.4% | 93.1% |
| GPT-4 | 86.4% | 67.0% | 92.0% | - |

### 中文能力对比

| 模型 | C-Eval | CMMLU | GAOKAO |
|------|--------|-------|--------|
| Qwen2.5 72B | 93.1% | 90.5% | 85.2% |
| LLaMA 3.1 70B | 78.3% | 75.1% | 68.9% |
| Mistral Large | 81.2% | 79.8% | 72.4% |
| GPT-4 | - | - | 82.1% |

### 代码能力对比

| 模型 | HumanEval | MBPP | LeetCode |
|------|-----------|------|----------|
| Qwen2.5-Coder 32B | 65.2% | 72.3% | 58.7% |
| GPT-4 | 67.0% | 75.0% | 62.1% |
| Claude 3.5 | 66.5% | 73.8% | 60.3% |
| LLaMA 3.1 70B | 58.2% | 65.4% | 52.1% |

## 🚀 使用方法

### Hugging Face 使用

```python
from transformers import AutoTokenizer, AutoModelForCausalLM

# 加载模型
model_name = "Qwen/Qwen2.5-7B-Instruct"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    device_map="auto",
    torch_dtype="auto"
)

# 准备输入
messages = [
    {"role": "system", "content": "You are a helpful assistant."},
    {"role": "user", "content": "解释量子计算的基本原理"}
]

# 应用聊天模板
text = tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
inputs = tokenizer(text, return_tensors="pt").to(model.device)

# 生成输出
outputs = model.generate(
    inputs.input_ids,
    max_new_tokens=512,
    temperature=0.7,
    top_p=0.9
)

# 解码输出
generated_ids = outputs[0][inputs.input_ids.shape[-1]:]
generated_text = tokenizer.decode(generated_ids, skip_special_tokens=True)
print(generated_text)
```

### 使用 Qwen2.5-Coder

```python
from transformers import AutoTokenizer, AutoModelForCausalLM

# 加载代码模型
model_name = "Qwen/Qwen2.5-Coder-7B-Instruct"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    device_map="auto",
    torch_dtype="auto"
)

# 代码生成任务
messages = [
    {"role": "system", "content": "You are a coding assistant."},
    {"role": "user", "content": "写一个 Python 函数实现快速排序"}
]

text = tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
inputs = tokenizer(text, return_tensors="pt").to(model.device)

outputs = model.generate(
    inputs.input_ids,
    max_new_tokens=512,
    temperature=0.2
)

generated_ids = outputs[0][inputs.input_ids.shape[-1]:]
generated_code = tokenizer.decode(generated_ids, skip_special_tokens=True)
print(generated_code)
```

### 使用 Qwen2-VL（多模态）

```python
from transformers import Qwen2VLForConditionalGeneration, AutoProcessor
from qwen_vl_utils import process_vision_info

# 加载多模态模型
model = Qwen2VLForConditionalGeneration.from_pretrained(
    "Qwen/Qwen2-VL-7B-Instruct",
    torch_dtype="auto",
    device_map="auto"
)
processor = AutoProcessor.from_pretrained("Qwen/Qwen2-VL-7B-Instruct")

# 准备输入（文本 + 图像）
messages = [
    {
        "role": "user",
        "content": [
            {"type": "image", "image": "image.jpg"},
            {"type": "text", "text": "描述这张图片的内容"},
        ],
    }
]

# 处理输入
text = processor.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
image_inputs, video_inputs = process_vision_info(messages)
inputs = processor(
    text=[text],
    images=image_inputs,
    videos=video_inputs,
    padding=True,
    return_tensors="pt",
).to(model.device)

# 生成输出
generated_ids = model.generate(**inputs, max_new_tokens=512)
generated_ids_trimmed = [
    out_ids[len(in_ids):] for in_ids, out_ids in zip(inputs.input_ids, generated_ids)
]
output_text = processor.batch_decode(
    generated_ids_trimmed, skip_special_tokens=True, clean_up_tokenization_spaces=False
)
print(output_text[0])
```

### API 使用（通义千问）

```python
import requests
import json

# 通义千问 API
url = "https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation"

headers = {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
}

data = {
    "model": "qwen-max",
    "input": {
        "messages": [
            {"role": "system", "content": "You are a helpful assistant."},
            {"role": "user", "content": "解释机器学习的基本概念"}
        ]
    },
    "parameters": {
        "max_tokens": 500,
        "temperature": 0.7
    }
}

response = requests.post(url, headers=headers, json=data)
result = response.json()
print(result['output']['text'])
```

## 🔧 微调 Qwen

### 使用 LoRA 微调

```python
from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments
from peft import LoraConfig, get_peft_model, TaskType
import torch

# 加载模型
model_name = "Qwen/Qwen2.5-7B-Instruct"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    device_map="auto",
    torch_dtype=torch.float16
)

# 配置 LoRA
lora_config = LoraConfig(
    task_type=TaskType.CAUSAL_LM,
    r=16,
    lora_alpha=32,
    lora_dropout=0.05,
    target_modules=["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"],
    bias="none"
)

# 应用 LoRA
model = get_peft_model(model, lora_config)

# 训练配置
training_args = TrainingArguments(
    output_dir="./qwen-finetuned",
    num_train_epochs=3,
    per_device_train_batch_size=4,
    gradient_accumulation_steps=4,
    learning_rate=2e-4,
    fp16=True,
    logging_steps=10,
    save_steps=100
)

# 开始训练
trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=train_dataset,
    tokenizer=tokenizer
)

trainer.train()
```

## 💡 最佳实践

### 模型选择

| 使用场景 | 推荐模型 | 硬件要求 |
|---------|---------|---------|
| 中文问答 | Qwen2.5 7B | 单张 16GB GPU |
| 代码生成 | Qwen2.5-Coder 7B | 单张 16GB GPU |
| 多模态 | Qwen2-VL 7B | 单张 24GB GPU |
| 高性能 | Qwen2.5 72B | 4-8张 A100 |
| 生产环境 | 通义千问 API | 无需 GPU |

### 优化技巧

1. **利用长上下文**
   - 适合长文档处理
   - 代码分析
   - 多轮对话

2. **量化部署**
   - 4-bit 量化减少内存
   - 使用 llama.cpp
   - 性能损失很小

3. **批处理优化**
   - 增加批大小
   - 使用梯度累积
   - 注意显存限制

4. **使用 Chat Template**
   - 自动格式化对话
   - 支持多轮对话
   - 提高生成质量

## 🌐 生态系统

### 工具和框架

- **Hugging Face Transformers**：完整支持
- **vLLM**：高性能推理
- **llama.cpp**：CPU/GPU 推理
- **Ollama**：本地部署
- **通义千问 API**：云端服务
- **ModelScope**：阿里云模型库

### 社区资源

- **官方 GitHub**：https://github.com/QwenLM
- **Hugging Face**：https://huggingface.co/Qwen
- **ModelScope**：https://modelscope.cn/home
- **阿里云文档**：详细的 API 文档

## 🔗 相关资源

### 官方资源
- 文档处理
- 文档处理
- 文档处理
- 文档处理

### 代码仓库
- 文档处理
- 文档处理
- 文档处理

### 学习资源
- 文档处理
- 文档处理
- 文档处理

## ✅ 学习检查点

- [ ] 理解 Qwen 系列的发展历程
- [ ] 掌握 Qwen 的架构特点
- [ ] 能够使用 Hugging Face 加载 Qwen
- [ ] 了解如何使用 Qwen2-VL 多模态
- [ ] 掌握 Qwen2.5-Coder 的使用
- [ ] 理解 Qwen 的中文优势

## 🚀 下一步

继续学习以下内容：
- Baichuan 系列模型
- ChatGLM 系列模型