# 数据验证

> **版本**：v1.0  
> **最后更新**：2026-04-21

## 📚 概念介绍

数据验证是前端开发中的重要环节，确保用户输入的数据符合预期格式和规则。React 生态中有多个强大的数据验证库，如 Zod、Yup、Joi 等。本章节将详细介绍这些验证库的使用方法和最佳实践。

## 🎯 Zod

### 基础验证

```jsx
import React, { useState } from 'react';
import { z } from 'zod';

// 定义验证 schema
const userSchema = z.object({
  username: z.string()
    .min(3, 'Username must be at least 3 characters')
    .max(20, 'Username must not exceed 20 characters')
    .regex(/^[a-zA-Z0-9_]+$/, 'Username can only contain letters, numbers, and underscores'),
  email: z.string()
    .email('Invalid email address'),
  age: z.number()
    .min(18, 'You must be at least 18 years old')
    .max(120, 'Please enter a valid age'),
  website: z.string()
    .url('Invalid URL')
    .optional(),
  bio: z.string()
    .max(500, 'Bio must not exceed 500 characters')
    .optional()
});

function ZodValidationExample() {
  const [formData, setFormData] = useState({
    username: '',
    email: '',
    age: '',
    website: '',
    bio: ''
  });
  const [errors, setErrors] = useState({});
  
  const handleChange = (e) => {
    const { name, value } = e.target;
    setFormData(prev => ({
      ...prev,
      [name]: value
    }));
    
    // 实时验证
    try {
      userSchema.parse({
        ...formData,
        [name]: value
      });
      setErrors(prev => ({ ...prev, [name]: null }));
    } catch (error) {
      if (error.errors) {
        const fieldError = error.errors.find(err => err.path[0] === name);
        setErrors(prev => ({ ...prev, [name]: fieldError?.message || null }));
      }
    }
  };
  
  const handleSubmit = (e) => {
    e.preventDefault();
    
    try {
      const validatedData = userSchema.parse({
        ...formData,
        age: formData.age ? parseInt(formData.age) : undefined
      });
      console.log('Validated data:', validatedData);
      alert('Form submitted successfully!');
      setErrors({});
    } catch (error) {
      if (error.errors) {
        const newErrors = {};
        error.errors.forEach(err => {
          newErrors[err.path[0]] = err.message;
        });
        setErrors(newErrors);
      }
    }
  };
  
  return (
    <div style={{ padding: '24px', maxWidth: '600px', margin: '0 auto' }}>
      <h2>Zod Validation</h2>
      <form onSubmit={handleSubmit}>
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Username:
          </label>
          <input
            type="text"
            name="username"
            value={formData.username}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.username ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.username && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.username}
            </p>
          )}
        </div>
        
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Email:
          </label>
          <input
            type="email"
            name="email"
            value={formData.email}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.email ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.email && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.email}
            </p>
          )}
        </div>
        
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Age:
          </label>
          <input
            type="number"
            name="age"
            value={formData.age}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.age ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.age && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.age}
            </p>
          )}
        </div>
        
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Website (optional):
          </label>
          <input
            type="url"
            name="website"
            value={formData.website}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.website ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.website && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.website}
            </p>
          )}
        </div>
        
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Bio (optional):
          </label>
          <textarea
            name="bio"
            value={formData.bio}
            onChange={handleChange}
            rows={4}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.bio ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.bio && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.bio}
            </p>
          )}
        </div>
        
        <button
          type="submit"
          style={{
            padding: '10px 20px',
            background: '#1890ff',
            color: 'white',
            border: 'none',
            borderRadius: '4px',
            cursor: 'pointer'
          }}
        >
          Submit
        </button>
      </form>
    </div>
  );
}

export default ZodValidationExample;
```

### 高级验证

```jsx
import React, { useState } from 'react';
import { z } from 'zod';

// 复杂的验证 schema
const advancedSchema = z.object({
  password: z.string()
    .min(8, 'Password must be at least 8 characters')
    .regex(/[A-Z]/, 'Password must contain at least one uppercase letter')
    .regex(/[a-z]/, 'Password must contain at least one lowercase letter')
    .regex(/[0-9]/, 'Password must contain at least one number'),
  confirmPassword: z.string(),
  tags: z.array(z.string().min(1)).min(1, 'At least one tag is required'),
  preferences: z.object({
    notifications: z.boolean(),
    theme: z.enum(['light', 'dark', 'auto']),
    language: z.enum(['en', 'zh', 'ja'])
  }),
  address: z.object({
    street: z.string().min(5, 'Street address must be at least 5 characters'),
    city: z.string().min(2, 'City must be at least 2 characters'),
    zipCode: z.string().regex(/^\d{5}(-\d{4})?$/, 'Invalid zip code')
  })
}).refine((data) => data.password === data.confirmPassword, {
  message: "Passwords don't match",
  path: ['confirmPassword']
});

function AdvancedZodExample() {
  const [formData, setFormData] = useState({
    password: '',
    confirmPassword: '',
    tags: '',
    preferences: {
      notifications: false,
      theme: 'light',
      language: 'en'
    },
    address: {
      street: '',
      city: '',
      zipCode: ''
    }
  });
  const [errors, setErrors] = useState({});
  
  const handleChange = (e) => {
    const { name, value, type, checked } = e.target;
    
    if (name.includes('.')) {
      const [parent, child] = name.split('.');
      setFormData(prev => ({
        ...prev,
        [parent]: {
          ...prev[parent],
          [child]: type === 'checkbox' ? checked : value
        }
      }));
    } else {
      setFormData(prev => ({
        ...prev,
        [name]: value
      }));
    }
  };
  
  const handleSubmit = (e) => {
    e.preventDefault();
    
    try {
      const validatedData = advancedSchema.parse({
        ...formData,
        tags: formData.tags.split(',').map(tag => tag.trim()).filter(tag => tag)
      });
      console.log('Validated data:', validatedData);
      alert('Form submitted successfully!');
      setErrors({});
    } catch (error) {
      if (error.errors) {
        const newErrors = {};
        error.errors.forEach(err => {
          const path = err.path.join('.');
          newErrors[path] = err.message;
        });
        setErrors(newErrors);
      }
    }
  };
  
  return (
    <div style={{ padding: '24px', maxWidth: '700px', margin: '0 auto' }}>
      <h2>Advanced Zod Validation</h2>
      <form onSubmit={handleSubmit}>
        {/* Password */}
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Password:
          </label>
          <input
            type="password"
            name="password"
            value={formData.password}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.password ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.password && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.password}
            </p>
          )}
        </div>
        
        {/* Confirm Password */}
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Confirm Password:
          </label>
          <input
            type="password"
            name="confirmPassword"
            value={formData.confirmPassword}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.confirmPassword ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.confirmPassword && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.confirmPassword}
            </p>
          )}
        </div>
        
        {/* Tags */}
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Tags (comma-separated):
          </label>
          <input
            type="text"
            name="tags"
            value={formData.tags}
            onChange={handleChange}
            placeholder="react, javascript, web"
            style={{
              width: '100%',
              padding: '8px',
              border: errors.tags ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.tags && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.tags}
            </p>
          )}
        </div>
        
        {/* Preferences */}
        <div style={{ marginBottom: '16px', padding: '16px', background: '#f5f5f5', borderRadius: '4px' }}>
          <h3>Preferences</h3>
          
          <div style={{ marginBottom: '12px' }}>
            <label>
              <input
                type="checkbox"
                name="preferences.notifications"
                checked={formData.preferences.notifications}
                onChange={handleChange}
                style={{ marginRight: '8px' }}
              />
              Enable notifications
            </label>
          </div>
          
          <div style={{ marginBottom: '12px' }}>
            <label style={{ display: 'block', marginBottom: '8px' }}>
              Theme:
            </label>
            <select
              name="preferences.theme"
              value={formData.preferences.theme}
              onChange={handleChange}
              style={{
                width: '100%',
                padding: '8px',
                border: '1px solid #ccc',
                borderRadius: '4px'
              }}
            >
              <option value="light">Light</option>
              <option value="dark">Dark</option>
              <option value="auto">Auto</option>
            </select>
          </div>
          
          <div>
            <label style={{ display: 'block', marginBottom: '8px' }}>
              Language:
            </label>
            <select
              name="preferences.language"
              value={formData.preferences.language}
              onChange={handleChange}
              style={{
                width: '100%',
                padding: '8px',
                border: '1px solid #ccc',
                borderRadius: '4px'
              }}
            >
              <option value="en">English</option>
              <option value="zh">中文</option>
              <option value="ja">日本語</option>
            </select>
          </div>
        </div>
        
        {/* Address */}
        <div style={{ marginBottom: '16px', padding: '16px', background: '#f5f5f5', borderRadius: '4px' }}>
          <h3>Address</h3>
          
          <div style={{ marginBottom: '12px' }}>
            <label style={{ display: 'block', marginBottom: '8px' }}>
              Street:
            </label>
            <input
              type="text"
              name="address.street"
              value={formData.address.street}
              onChange={handleChange}
              style={{
                width: '100%',
                padding: '8px',
                border: errors['address.street'] ? '1px solid red' : '1px solid #ccc',
                borderRadius: '4px'
              }}
            />
            {errors['address.street'] && (
              <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
                {errors['address.street']}
              </p>
            )}
          </div>
          
          <div style={{ marginBottom: '12px' }}>
            <label style={{ display: 'block', marginBottom: '8px' }}>
              City:
            </label>
            <input
              type="text"
              name="address.city"
              value={formData.address.city}
              onChange={handleChange}
              style={{
                width: '100%',
                padding: '8px',
                border: errors['address.city'] ? '1px solid red' : '1px solid #ccc',
                borderRadius: '4px'
              }}
            />
            {errors['address.city'] && (
              <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
                {errors['address.city']}
              </p>
            )}
          </div>
          
          <div>
            <label style={{ display: 'block', marginBottom: '8px' }}>
              Zip Code:
            </label>
            <input
              type="text"
              name="address.zipCode"
              value={formData.address.zipCode}
              onChange={handleChange}
              placeholder="12345 or 12345-6789"
              style={{
                width: '100%',
                padding: '8px',
                border: errors['address.zipCode'] ? '1px solid red' : '1px solid #ccc',
                borderRadius: '4px'
              }}
            />
            {errors['address.zipCode'] && (
              <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
                {errors['address.zipCode']}
              </p>
            )}
          </div>
        </div>
        
        <button
          type="submit"
          style={{
            padding: '10px 20px',
            background: '#1890ff',
            color: 'white',
            border: 'none',
            borderRadius: '4px',
            cursor: 'pointer'
          }}
        >
          Submit
        </button>
      </form>
    </div>
  );
}

export default AdvancedZodExample;
```

## 🎯 Yup

### 基础验证

```jsx
import React, { useState } from 'react';
import * as yup from 'yup';

// 定义验证 schema
const userSchema = yup.object().shape({
  username: yup.string()
    .required('Username is required')
    .min(3, 'Username must be at least 3 characters')
    .max(20, 'Username must not exceed 20 characters')
    .matches(/^[a-zA-Z0-9_]+$/, 'Username can only contain letters, numbers, and underscores'),
  email: yup.string()
    .required('Email is required')
    .email('Invalid email address'),
  age: yup.number()
    .required('Age is required')
    .min(18, 'You must be at least 18 years old')
    .max(120, 'Please enter a valid age'),
  phone: yup.string()
    .required('Phone number is required')
    .matches(/^1[3-9]\d{9}$/, 'Please enter a valid phone number')
});

function YupValidationExample() {
  const [formData, setFormData] = useState({
    username: '',
    email: '',
    age: '',
    phone: ''
  });
  const [errors, setErrors] = useState({});
  
  const handleChange = (e) => {
    const { name, value } = e.target;
    setFormData(prev => ({
      ...prev,
      [name]: value
    }));
    
    // 实时验证
    yup.reach(userSchema, name)
      .validate(value)
      .then(() => {
        setErrors(prev => ({ ...prev, [name]: null }));
      })
      .catch((err) => {
        setErrors(prev => ({ ...prev, [name]: err.message }));
      });
  };
  
  const handleSubmit = async (e) => {
    e.preventDefault();
    
    try {
      const validatedData = await userSchema.validate({
        ...formData,
        age: formData.age ? parseInt(formData.age) : undefined
      }, { abortEarly: false });
      
      console.log('Validated data:', validatedData);
      alert('Form submitted successfully!');
      setErrors({});
    } catch (error) {
      if (error.inner) {
        const newErrors = {};
        error.inner.forEach(err => {
          newErrors[err.path] = err.message;
        });
        setErrors(newErrors);
      }
    }
  };
  
  return (
    <div style={{ padding: '24px', maxWidth: '600px', margin: '0 auto' }}>
      <h2>Yup Validation</h2>
      <form onSubmit={handleSubmit}>
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Username:
          </label>
          <input
            type="text"
            name="username"
            value={formData.username}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.username ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.username && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.username}
            </p>
          )}
        </div>
        
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Email:
          </label>
          <input
            type="email"
            name="email"
            value={formData.email}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.email ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.email && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.email}
            </p>
          )}
        </div>
        
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Age:
          </label>
          <input
            type="number"
            name="age"
            value={formData.age}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '8px',
              border: errors.age ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.age && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.age}
            </p>
          )}
        </div>
        
        <div style={{ marginBottom: '16px' }}>
          <label style={{ display: 'block', marginBottom: '8px' }}>
            Phone:
          </label>
          <input
            type="tel"
            name="phone"
            value={formData.phone}
            onChange={handleChange}
            placeholder="13800138000"
            style={{
              width: '100%',
              padding: '8px',
              border: errors.phone ? '1px solid red' : '1px solid #ccc',
              borderRadius: '4px'
            }}
          />
          {errors.phone && (
            <p style={{ color: 'red', fontSize: '12px', marginTop: '4px' }}>
              {errors.phone}
            </p>
          )}
        </div>
        
        <button
          type="submit"
          style={{
            padding: '10px 20px',
            background: '#1890ff',
            color: 'white',
            border: 'none',
            borderRadius: '4px',
            cursor: 'pointer'
          }}
        >
          Submit
        </button>
      </form>
    </div>
  );
}

export default YupValidationExample;
```

## 🎯 验证库对比

### Zod vs Yup vs Joi

```jsx
import React from 'react';

function ValidationLibraryComparison() {
  const libraries = [
    {
      name: 'Zod',
      description: 'TypeScript-first schema validation with static type inference',
      pros: [
        'TypeScript 原生支持',
        '自动类型推断',
        '性能优秀',
        'API 简洁直观',
        '支持异步验证'
      ],
      cons: [
        '相对较新',
        '社区规模较小'
      ],
      useCase: 'TypeScript 项目，需要类型安全'
    },
    {
      name: 'Yup',
      description: 'Simple JavaScript object schema validation',
      pros: [
        '成熟稳定',
        '社区活跃',
        '文档完善',
        '与 Formik 集成良好',
        'API 易于理解'
      ],
      cons: [
        'TypeScript 支持需要额外配置',
        '性能略逊于 Zod'
      ],
      useCase: 'JavaScript 项目，或与 Formik 配合使用'
    },
    {
      name: 'Joi',
      description: 'Object schema validation',
      pros: [
        '功能强大',
        'API 丰富',
        '灵活度高',
        '支持复杂验证规则'
      ],
      cons: [
        '包体积较大',
        '性能一般',
        '学习曲线较陡'
      ],
      useCase: '复杂的验证场景，需要高度定制化'
    }
  ];
  
  return (
    <div style={{ padding: '24px', maxWidth: '800px', margin: '0 auto' }}>
      <h1>验证库对比</h1>
      
      {libraries.map((lib, index) => (
        <div
          key={index}
          style={{
            marginBottom: '24px',
            padding: '16px',
            border: '1px solid #ddd',
            borderRadius: '8px',
            background: index % 2 === 0 ? '#f9f9f9' : 'white'
          }}
        >
          <h2 style={{ color: '#1890ff' }}>{lib.name}</h2>
          <p style={{ marginBottom: '12px', fontStyle: 'italic' }}>
            {lib.description}
          </p>
          
          <div style={{ marginBottom: '12px' }}>
            <h3>优点：</h3>
            <ul>
              {lib.pros.map((pro, i) => (
                <li key={i}>{pro}</li>
              ))}
            </ul>
          </div>
          
          <div style={{ marginBottom: '12px' }}>
            <h3>缺点：</h3>
            <ul>
              {lib.cons.map((con, i) => (
                <li key={i}>{con}</li>
              ))}
            </ul>
          </div>
          
          <div>
            <h3>适用场景：</h3>
            <p style={{ fontWeight: 'bold', color: '#52c41a' }}>
              {lib.useCase}
            </p>
          </div>
        </div>
      ))}
      
      <div style={{ marginTop: '32px', padding: '16px', background: '#e6f7ff', borderRadius: '8px', border: '1px solid #91d5ff' }}>
        <h2>选择建议</h2>
        <ul>
          <li><strong>TypeScript 项目</strong>：优先选择 Zod，类型安全且性能优秀</li>
          <li><strong>JavaScript 项目</strong>：选择 Yup，成熟稳定且易于使用</li>
          <li><strong>复杂验证</strong>：选择 Joi，功能强大且灵活度高</li>
          <li><strong>Formik 用户</strong>：选择 Yup，集成最佳</li>
          <li><strong>性能敏感</strong>：选择 Zod，性能最优</li>
        </ul>
      </div>
    </div>
  );
}

export default ValidationLibraryComparison;
```

## 📝 实际应用示例

### 综合示例：注册表单验证

```jsx
import React, { useState } from 'react';
import { z } from 'zod';
import { Button, Input, Form, message } from 'antd';

// 定义验证 schema
const registrationSchema = z.object({
  username: z.string()
    .min(3, 'Username must be at least 3 characters')
    .max(20, 'Username must not exceed 20 characters')
    .regex(/^[a-zA-Z0-9_]+$/, 'Username can only contain letters, numbers, and underscores'),
  email: z.string()
    .email('Invalid email address'),
  password: z.string()
    .min(8, 'Password must be at least 8 characters')
    .regex(/[A-Z]/, 'Password must contain at least one uppercase letter')
    .regex(/[a-z]/, 'Password must contain at least one lowercase letter')
    .regex(/[0-9]/, 'Password must contain at least one number'),
  confirmPassword: z.string(),
  firstName: z.string()
    .min(2, 'First name must be at least 2 characters')
    .regex(/^[a-zA-Z\s]+$/, 'First name can only contain letters'),
  lastName: z.string()
    .min(2, 'Last name must be at least 2 characters')
    .regex(/^[a-zA-Z\s]+$/, 'Last name can only contain letters'),
  agreeToTerms: z.literal(true, {
    errorMap: () => ({ message: 'You must agree to the terms and conditions' })
  })
}).refine((data) => data.password === data.confirmPassword, {
  message: "Passwords don't match",
  path: ['confirmPassword']
});

function RegistrationFormWithZod() {
  const [formData, setFormData] = useState({
    username: '',
    email: '',
    password: '',
    confirmPassword: '',
    firstName: '',
    lastName: '',
    agreeToTerms: false
  });
  const [errors, setErrors] = useState({});
  const [loading, setLoading] = useState(false);
  
  const handleChange = (e) => {
    const { name, value, type, checked } = e.target;
    setFormData(prev => ({
      ...prev,
      [name]: type === 'checkbox' ? checked : value
    }));
  };
  
  const validateField = (name, value) => {
    try {
      registrationSchema.parse({
        ...formData,
        [name]: value
      });
      setErrors(prev => ({ ...prev, [name]: null }));
      return true;
    } catch (error) {
      if (error.errors) {
        const fieldError = error.errors.find(err => err.path[0] === name);
        setErrors(prev => ({ ...prev, [name]: fieldError?.message || null }));
      }
      return false;
    }
  };
  
  const handleBlur = (e) => {
    const { name, value } = e.target;
    validateField(name, value);
  };
  
  const handleSubmit = async (e) => {
    e.preventDefault();
    setLoading(true);
    
    try {
      const validatedData = registrationSchema.parse(formData);
      console.log('Validated data:', validatedData);
      
      // 模拟 API 调用
      await new Promise(resolve => setTimeout(resolve, 1500));
      
      message.success('Registration successful!');
      setErrors({});
      setFormData({
        username: '',
        email: '',
        password: '',
        confirmPassword: '',
        firstName: '',
        lastName: '',
        agreeToTerms: false
      });
    } catch (error) {
      if (error.errors) {
        const newErrors = {};
        error.errors.forEach(err => {
          newErrors[err.path[0]] = err.message;
        });
        setErrors(newErrors);
        message.error('Please fix the errors before submitting');
      }
    } finally {
      setLoading(false);
    }
  };
  
  return (
    <div style={{ padding: '24px', maxWidth: '600px', margin: '0 auto' }}>
      <h2>Registration Form</h2>
      <Form layout="vertical" onFinish={handleSubmit}>
        <Form.Item
          label="Username"
          validateStatus={errors.username ? 'error' : ''}
          help={errors.username}
        >
          <Input
            name="username"
            value={formData.username}
            onChange={handleChange}
            onBlur={handleBlur}
            placeholder="Choose a username"
          />
        </Form.Item>
        
        <Form.Item
          label="Email"
          validateStatus={errors.email ? 'error' : ''}
          help={errors.email}
        >
          <Input
            name="email"
            type="email"
            value={formData.email}
            onChange={handleChange}
            onBlur={handleBlur}
            placeholder="Enter your email"
          />
        </Form.Item>
        
        <Form.Item
          label="Password"
          validateStatus={errors.password ? 'error' : ''}
          help={errors.password}
        >
          <Input.Password
            name="password"
            value={formData.password}
            onChange={handleChange}
            onBlur={handleBlur}
            placeholder="Create a password"
          />
        </Form.Item>
        
        <Form.Item
          label="Confirm Password"
          validateStatus={errors.confirmPassword ? 'error' : ''}
          help={errors.confirmPassword}
        >
          <Input.Password
            name="confirmPassword"
            value={formData.confirmPassword}
            onChange={handleChange}
            onBlur={handleBlur}
            placeholder="Confirm your password"
          />
        </Form.Item>
        
        <Form.Item
          label="First Name"
          validateStatus={errors.firstName ? 'error' : ''}
          help={errors.firstName}
        >
          <Input
            name="firstName"
            value={formData.firstName}
            onChange={handleChange}
            onBlur={handleBlur}
            placeholder="Enter your first name"
          />
        </Form.Item>
        
        <Form.Item
          label="Last Name"
          validateStatus={errors.lastName ? 'error' : ''}
          help={errors.lastName}
        >
          <Input
            name="lastName"
            value={formData.lastName}
            onChange={handleChange}
            onBlur={handleBlur}
            placeholder="Enter your last name"
          />
        </Form.Item>
        
        <Form.Item
          validateStatus={errors.agreeToTerms ? 'error' : ''}
          help={errors.agreeToTerms}
        >
          <label>
            <input
              type="checkbox"
              name="agreeToTerms"
              checked={formData.agreeToTerms}
              onChange={handleChange}
              style={{ marginRight: '8px' }}
            />
            I agree to the terms and conditions
          </label>
        </Form.Item>
        
        <Form.Item>
          <Button
            type="primary"
            htmlType="submit"
            block
            loading={loading}
            size="large"
          >
            Register
          </Button>
        </Form.Item>
      </Form>
    </div>
  );
}

export default RegistrationFormWithZod;
```

## 🔗 相关概念

- **React Hook Form**：高性能表单库
- **Formik**：表单状态管理
- **表单验证**：前端数据验证
- **类型安全**：TypeScript 集成

## 📚 学习检查点

- [ ] 理解数据验证的重要性
- [ ] 掌握 Zod 的使用方法
- [ ] 掌握 Yup 的使用方法
- [ ] 了解不同验证库的特点
- [ ] 能够选择合适的验证库
- [ ] 能够实现复杂的验证逻辑

## 🚀 下一步

→ [React 19 表单](./03-react-19-forms) - 学习 React 19 表单特性