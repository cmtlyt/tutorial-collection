# 事件系统

> **版本**：v1.0  
> **最后更新**：2026-04-21

## 📚 概念介绍

React 使用合成事件（SyntheticEvent）系统来处理用户交互，它统一了不同浏览器的事件行为，提供了一致的事件处理接口。本章节将详细介绍 React 的事件系统、事件处理机制和最佳实践。

## 🎯 合成事件（SyntheticEvent）

### 基本概念

React 的合成事件是对原生 DOM 事件的封装，它提供了跨浏览器的兼容性：

```jsx
import React from 'react';

function Button() {
  const handleClick = (event) => {
    // event 是 SyntheticEvent 对象
    console.log('Event type:', event.type);
    console.log('Event target:', event.target);
    console.log('Event current target:', event.currentTarget);
    console.log('Native event:', event.nativeEvent);
  };
  
  return <button onClick={handleClick}>Click me</button>;
}

export default Button;
```

### SyntheticEvent 的属性

```jsx
import React from 'react';

function EventProperties() {
  const handleClick = (event) => {
    console.log('Event Properties:');
    console.log('- type:', event.type);
    console.log('- target:', event.target);
    console.log('- currentTarget:', event.currentTarget);
    console.log('- bubbles:', event.bubbles);
    console.log('- cancelable:', event.cancelable);
    console.log('- defaultPrevented:', event.defaultPrevented);
    console.log('- eventPhase:', event.eventPhase);
    console.log('- isTrusted:', event.isTrusted);
    console.log('- timeStamp:', event.timeStamp);
    console.log('- nativeEvent:', event.nativeEvent);
  };
  
  return <button onClick={handleClick}>Show Event Properties</button>;
}

export default EventProperties;
```

### 阻止默认行为和事件冒泡

```jsx
import React from 'react';

function EventControl() {
  const handleLinkClick = (event) => {
    event.preventDefault(); // 阻止默认行为（跳转）
    console.log('Link clicked, but navigation prevented');
  };
  
  const handleButtonClick = (event) => {
    event.stopPropagation(); // 阻止事件冒泡
    console.log('Button clicked, propagation stopped');
  };
  
  return (
    <div onClick={() => console.log('Div clicked')}>
      <a href="https://example.com" onClick={handleLinkClick}>
        Click me (won't navigate)
      </a>
      
      <button onClick={handleButtonClick}>
        Click me (won't bubble)
      </button>
    </div>
  );
}

export default EventControl;
```

## 🎯 事件委托机制

### 事件委托原理

React 使用事件委托来提高性能，它在根节点上监听事件，而不是在每个元素上：

```jsx
import React from 'react';

function EventDelegation() {
  const handleItemClick = (event) => {
    // 检查点击的是否是列表项
    if (event.target.tagName === 'LI') {
      console.log('Clicked item:', event.target.textContent);
    }
  };
  
  return (
    <ul onClick={handleItemClick}>
      <li>Item 1</li>
      <li>Item 2</li>
      <li>Item 3</li>
    </ul>
  );
}

export default EventDelegation;
```

### 动态元素的事件处理

```jsx
import React, { useState } from 'react';

function DynamicList() {
  const [items, setItems] = useState(['Item 1', 'Item 2', 'Item 3']);
  
  const addItem = () => {
    const newItem = `Item ${items.length + 1}`;
    setItems([...items, newItem]);
  };
  
  const handleItemClick = (event) => {
    if (event.target.tagName === 'LI') {
      console.log('Clicked:', event.target.textContent);
    }
  };
  
  return (
    <div>
      <button onClick={addItem}>Add Item</button>
      <ul onClick={handleItemClick}>
        {items.map((item, index) => (
          <li key={index}>{item}</li>
        ))}
      </ul>
    </div>
  );
}

export default DynamicList;
```

## 🎯 事件对象与阻止默认行为

### 鼠标事件

```jsx
import React from 'react';

function MouseEventDemo() {
  const handleMouseEvents = (event) => {
    console.log('Mouse Event:', event.type);
    console.log('- Client X/Y:', event.clientX, event.clientY);
    console.log('- Screen X/Y:', event.screenX, event.screenY);
    console.log('- Page X/Y:', event.pageX, event.pageY);
    console.log('- Button:', event.button);
    console.log('- Buttons:', event.buttons);
    console.log('- Alt Key:', event.altKey);
    console.log('- Ctrl Key:', event.ctrlKey);
    console.log('- Shift Key:', event.shiftKey);
    console.log('- Meta Key:', event.metaKey);
  };
  
  return (
    <div>
      <button
        onClick={handleMouseEvents}
        onMouseDown={handleMouseEvents}
        onMouseUp={handleMouseEvents}
        onMouseEnter={handleMouseEvents}
        onMouseLeave={handleMouseEvents}
        onMouseMove={handleMouseEvents}
      >
        Mouse Events Demo
      </button>
    </div>
  );
}

export default MouseEventDemo;
```

### 键盘事件

```jsx
import React, { useState } from 'react';

function KeyEventDemo() {
  const [keyInfo, setKeyInfo] = useState('');
  
  const handleKeyDown = (event) => {
    const info = `
      Key: ${event.key}
      Code: ${event.code}
      KeyCode: ${event.keyCode}
      Which: ${event.which}
      Location: ${event.location}
      Repeat: ${event.repeat}
      Alt: ${event.altKey}
      Ctrl: ${event.ctrlKey}
      Shift: ${event.shiftKey}
      Meta: ${event.metaKey}
    `;
    setKeyInfo(info);
    
    // 阻止某些按键的默认行为
    if (event.key === 'Enter') {
      event.preventDefault();
      console.log('Enter key pressed, default prevented');
    }
  };
  
  return (
    <div>
      <input
        type="text"
        onKeyDown={handleKeyDown}
        placeholder="Press any key"
      />
      <pre>{keyInfo}</pre>
    </div>
  );
}

export default KeyEventDemo;
```

### 表单事件

```jsx
import React, { useState } from 'react';

function FormEventDemo() {
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    message: ''
  });
  
  const handleChange = (event) => {
    const { name, value } = event.target;
    setFormData(prev => ({ ...prev, [name]: value }));
  };
  
  const handleSubmit = (event) => {
    event.preventDefault();
    console.log('Form submitted:', formData);
  };
  
  const handleFocus = (event) => {
    console.log('Focused:', event.target.name);
  };
  
  const handleBlur = (event) => {
    console.log('Blurred:', event.target.name);
  };
  
  return (
    <form onSubmit={handleSubmit}>
      <input
        name="name"
        type="text"
        value={formData.name}
        onChange={handleChange}
        onFocus={handleFocus}
        onBlur={handleBlur}
        placeholder="Name"
        required
      />
      
      <input
        name="email"
        type="email"
        value={formData.email}
        onChange={handleChange}
        onFocus={handleFocus}
        onBlur={handleBlur}
        placeholder="Email"
        required
      />
      
      <textarea
        name="message"
        value={formData.message}
        onChange={handleChange}
        onFocus={handleFocus}
        onBlur={handleBlur}
        placeholder="Message"
      />
      
      <button type="submit">Submit</button>
    </form>
  );
}

export default FormEventDemo;
```

## 🎯 自定义事件处理

### 传递参数

```jsx
import React from 'react';

function ParameterPassing() {
  const handleButtonClick = (message, event) => {
    console.log('Message:', message);
    console.log('Event:', event);
  };
  
  return (
    <div>
      <button onClick={(event) => handleButtonClick('Hello', event)}>
        Click me (Arrow function)
      </button>
      
      <button onClick={handleButtonClick.bind(null, 'World')}>
        Click me (bind)
      </button>
    </div>
  );
}

export default ParameterPassing;
```

### 事件处理器绑定

```jsx
import React, { useState } from 'react';

class EventBinding extends React.Component {
  constructor(props) {
    super(props);
    this.state = { count: 0 };
    
    // 方式 1：在构造函数中绑定
    this.handleClick1 = this.handleClick1.bind(this);
  }
  
  // 方式 1：在构造函数中绑定
  handleClick1() {
    this.setState(prev => ({ count: prev.count + 1 }));
  }
  
  // 方式 2：使用箭头函数
  handleClick2 = () => {
    this.setState(prev => ({ count: prev.count + 1 }));
  };
  
  // 方式 3：在 JSX 中使用箭头函数
  handleClick3() {
    this.setState(prev => ({ count: prev.count + 1 }));
  }
  
  render() {
    return (
      <div>
        <h2>Count: {this.state.count}</h2>
        <button onClick={this.handleClick1}>Method 1</button>
        <button onClick={this.handleClick2}>Method 2</button>
        <button onClick={() => this.handleClick3()}>Method 3</button>
      </div>
    );
  }
}

export default EventBinding;
```

## 📝 实际应用示例

### 综合示例：交互式画板

```jsx
import React, { useState, useRef, useEffect } from 'react';

function DrawingCanvas() {
  const canvasRef = useRef(null);
  const [isDrawing, setIsDrawing] = useState(false);
  const [color, setColor] = useState('#000000');
  const [lineWidth, setLineWidth] = useState(2);
  const [history, setHistory] = useState([]);
  const [historyIndex, setHistoryIndex] = useState(-1);
  
  useEffect(() => {
    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d');
    
    // 设置初始样式
    ctx.lineCap = 'round';
    ctx.lineJoin = 'round';
    ctx.strokeStyle = color;
    ctx.lineWidth = lineWidth;
    
    // 保存初始状态
    saveHistory();
  }, []);
  
  const saveHistory = () => {
    const canvas = canvasRef.current;
    const imageData = canvas.toDataURL();
    
    const newHistory = history.slice(0, historyIndex + 1);
    newHistory.push(imageData);
    
    setHistory(newHistory);
    setHistoryIndex(newHistory.length - 1);
  };
  
  const undo = () => {
    if (historyIndex > 0) {
      setHistoryIndex(historyIndex - 1);
      const canvas = canvasRef.current;
      const ctx = canvas.getContext('2d');
      const img = new Image();
      img.onload = () => {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        ctx.drawImage(img, 0, 0);
      };
      img.src = history[historyIndex - 1];
    }
  };
  
  const redo = () => {
    if (historyIndex < history.length - 1) {
      setHistoryIndex(historyIndex + 1);
      const canvas = canvasRef.current;
      const ctx = canvas.getContext('2d');
      const img = new Image();
      img.onload = () => {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        ctx.drawImage(img, 0, 0);
      };
      img.src = history[historyIndex + 1];
    }
  };
  
  const clearCanvas = () => {
    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d');
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    saveHistory();
  };
  
  const getMousePos = (event) => {
    const canvas = canvasRef.current;
    const rect = canvas.getBoundingClientRect();
    return {
      x: event.clientX - rect.left,
      y: event.clientY - rect.top
    };
  };
  
  const startDrawing = (event) => {
    setIsDrawing(true);
    const pos = getMousePos(event);
    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d');
    ctx.beginPath();
    ctx.moveTo(pos.x, pos.y);
  };
  
  const draw = (event) => {
    if (!isDrawing) return;
    
    const pos = getMousePos(event);
    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d');
    
    ctx.strokeStyle = color;
    ctx.lineWidth = lineWidth;
    
    ctx.lineTo(pos.x, pos.y);
    ctx.stroke();
  };
  
  const stopDrawing = () => {
    if (isDrawing) {
      setIsDrawing(false);
      saveHistory();
    }
  };
  
  return (
    <div className="drawing-canvas">
      <h1>Drawing Canvas</h1>
      
      <div className="controls">
        <div>
          <label>Color:</label>
          <input
            type="color"
            value={color}
            onChange={(e) => setColor(e.target.value)}
          />
        </div>
        
        <div>
          <label>Line Width:</label>
          <input
            type="range"
            min="1"
            max="20"
            value={lineWidth}
            onChange={(e) => setLineWidth(parseInt(e.target.value))}
          />
          <span>{lineWidth}px</span>
        </div>
        
        <div className="buttons">
          <button onClick={undo} disabled={historyIndex <= 0}>
            Undo
          </button>
          <button onClick={redo} disabled={historyIndex >= history.length - 1}>
            Redo
          </button>
          <button onClick={clearCanvas}>Clear</button>
        </div>
      </div>
      
      <canvas
        ref={canvasRef}
        width={800}
        height={600}
        onMouseDown={startDrawing}
        onMouseMove={draw}
        onMouseUp={stopDrawing}
        onMouseLeave={stopDrawing}
        style={{ border: '1px solid #ccc', cursor: 'crosshair' }}
      />
    </div>
  );
}

export default DrawingCanvas;
```

## 🔗 相关概念

- **表单处理**：受控组件和非受控组件
- **React 19 Actions**：异步转换和表单处理
- **事件委托**：性能优化技术
- **合成事件**：跨浏览器兼容性

## 📚 学习检查点

- [ ] 理解 SyntheticEvent 的概念和属性
- [ ] 掌握阻止默认行为和事件冒泡
- [ ] 理解事件委托的原理和应用
- [ ] 掌握鼠标事件、键盘事件和表单事件
- [ ] 能够传递参数到事件处理器
- [ ] 了解自定义事件处理的最佳实践

## 🚀 下一步

→ [表单处理](./02-form-handling) - 学习 React 19 的表单处理和 Actions