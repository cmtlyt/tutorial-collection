# Vite 构建工具详解

> **版本**: v1.0  
> **最后更新**: 2026-04-22

## 📚 概念介绍

Vite 是新一代前端构建工具，由 Vue.js 作者尤雨溪开发。它利用浏览器原生 ESM 模块系统实现极速的开发服务器启动，使用 Rollup 进行生产环境打包。Vite 6.0 进一步提升了性能，增强了对 React 19 的支持，并引入了更多现代化特性。

## 🎯 Vite 核心特性

### 1. 极速开发服务器

Vite 的开发服务器基于 esbuild，启动速度极快：

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    open: true,
    host: true
  }
});
```

### 2. 即时热模块替换（HMR）

Vite 提供模块级别的热替换，保持应用状态：

```jsx
// src/App.jsx
import { useState } from 'react';

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <h1>Vite + React 19</h1>
      <button onClick={() => setCount(count + 1)}>
        Count: {count}
      </button>
    </div>
  );
}

export default App;
```

### 3. React 19 支持

Vite 6.0 完全支持 React 19 的新特性：

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [
    react({
      babel: {
        plugins: [
          // React 19 新 JSX Transform 自动启用
          // 无需手动配置，Vite 自动处理
        ]
      }
    })
  ],
  optimizeDeps: {
    include: ['react', 'react-dom']
  }
});
```

## 🛠️ 完整配置示例

### 基础配置

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      '@components': path.resolve(__dirname, './src/components'),
      '@utils': path.resolve(__dirname, './src/utils')
    }
  },
  css: {
    modules: {
      localsConvention: 'camelCase'
    }
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          'ui-vendor': ['@mui/material', '@emotion/react']
        }
      }
    }
  }
});
```

### TypeScript 配置

```json
// tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@components/*": ["./src/components/*"],
      "@utils/*": ["./src/utils/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

```json
// tsconfig.node.json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.js"]
}
```

## 📝 实际应用示例

### 1. React 19 项目初始化

```bash
# 创建新项目
npm create vite@latest my-react19-app -- --template react-ts

cd my-react19-app

# 安装 React 19
npm install react@19 react-dom@19

# 安装类型定义
npm install -D @types/react@19 @types/react-dom@19
```

### 2. 环境变量配置

```js
// .env.development
VITE_API_URL=http://localhost:3001/api
VITE_APP_TITLE=React 19 Dev
VITE_ENABLE_DEBUG=true
```

```js
// .env.production
VITE_API_URL=https://api.example.com
VITE_APP_TITLE=React 19 App
VITE_ENABLE_DEBUG=false
```

```js
// src/config/env.js
const config = {
  apiUrl: import.meta.env.VITE_API_URL,
  appTitle: import.meta.env.VITE_APP_TITLE,
  enableDebug: import.meta.env.VITE_ENABLE_DEBUG === 'true'
};

export default config;
```

### 3. 代码分割配置

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  build: {
    rollupOptions: {
      output: {
        manualChunks: (id) => {
          if (id.includes('node_modules')) {
            if (id.includes('react') || id.includes('react-dom')) {
              return 'react-vendor';
            }
            if (id.includes('@mui') || id.includes('@emotion')) {
              return 'mui-vendor';
            }
            if (id.includes('lodash') || id.includes('axios')) {
              return 'utils-vendor';
            }
            return 'vendor';
          }
        }
      }
    },
    chunkSizeWarningLimit: 1000
  }
});
```

### 4. 开发服务器代理配置

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:3001',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '')
      },
      '/ws': {
        target: 'ws://localhost:3001',
        ws: true
      }
    }
  }
});
```

### 5. 图片优化配置

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import viteImagemin from 'vite-plugin-imagemin';

export default defineConfig({
  plugins: [
    react(),
    viteImagemin({
      gifsicle: { optimizationLevel: 7 },
      optipng: { optimizationLevel: 7 },
      mozjpeg: { quality: 80 },
      pngquant: { quality: [0.8, 0.9] },
      svgo: {
        plugins: [
          { name: 'removeViewBox', active: false },
          { name: 'removeEmptyAttrs', active: false }
        ]
      }
    })
  ]
});
```

### 6. PWA 支持

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.ico', 'apple-touch-icon.png'],
      manifest: {
        name: 'React 19 App',
        short_name: 'React19',
        description: 'A React 19 application built with Vite',
        theme_color: '#ffffff',
        background_color: '#ffffff',
        display: 'standalone',
        icons: [
          {
            src: '/icon-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: '/icon-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          }
        ]
      }
    })
  ]
});
```

## 🎨 样式处理

### CSS Modules

```jsx
// src/components/Button.module.css
.button {
  padding: 12px 24px;
  border: none;
  border-radius: 8px;
  background-color: #1976d2;
  color: white;
  font-size: 16px;
  cursor: pointer;
  transition: background-color 0.2s;
}

.button:hover {
  background-color: #1565c0;
}
```

```jsx
// src/components/Button.jsx
import styles from './Button.module.css';

function Button({ children, onClick }) {
  return (
    <button className={styles.button} onClick={onClick}>
      {children}
    </button>
  );
}

export default Button;
```

### Tailwind CSS 集成

```bash
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

```js
// tailwind.config.js
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

```css
/* src/index.css */
@tailwind base;
@tailwind components;
@tailwind utilities;
```

## 🚀 性能优化

### 1. 预加载优化

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'react-core': ['react', 'react-dom'],
          'router': ['react-router-dom']
        }
      }
    }
  }
});
```

### 2. 缓存策略

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  build: {
    rollupOptions: {
      output: {
        entryFileNames: 'assets/[name].[hash].js',
        chunkFileNames: 'assets/[name].[hash].js',
        assetFileNames: 'assets/[name].[hash].[ext]'
      }
    }
  }
});
```

### 3. 压缩优化

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import viteCompression from 'vite-plugin-compression';

export default defineConfig({
  plugins: [
    react(),
    viteCompression({
      algorithm: 'gzip',
      ext: '.gz'
    }),
    viteCompression({
      algorithm: 'brotliCompress',
      ext: '.br'
    })
  ]
});
```

## 📊 构建分析

```bash
# 安装分析工具
npm install -D rollup-plugin-visualizer
```

```js
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { visualizer } from 'rollup-plugin-visualizer';

export default defineConfig({
  plugins: [
    react(),
    visualizer({
      open: true,
      gzipSize: true,
      brotliSize: true
    })
  ]
});
```

## 🧪 测试配置

```bash
# 安装 Vitest
npm install -D vitest @vitest/ui jsdom @testing-library/react @testing-library/jest-dom
```

```js
// vitest.config.js
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: './src/test/setup.js'
  }
});
```

```js
// src/test/setup.js
import '@testing-library/jest-dom';
```

```jsx
// src/components/__tests__/Button.test.jsx
import { render, screen, fireEvent } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import Button from '../Button';

describe('Button', () => {
  it('renders button with text', () => {
    render(<Button>Click me</Button>);
    expect(screen.getByText('Click me')).toBeInTheDocument();
  });

  it('calls onClick when clicked', () => {
    const handleClick = vi.fn();
    render(<Button onClick={handleClick}>Click me</Button>);
    
    fireEvent.click(screen.getByText('Click me'));
    expect(handleClick).toHaveBeenCalledTimes(1);
  });
});
```

## 🔗 相关概念

- **Webpack**: 功能更全面的构建工具，适合复杂项目
- **Rollup**: Vite 生产环境打包的基础，专注于 ESM
- **esbuild**: Vite 开发服务器的核心，极速编译
- **React 19 新 JSX Transform**: Vite 自动支持，无需手动配置

## 📚 学习检查点

- [ ] 理解 Vite 的工作原理和优势
- [ ] 掌握 Vite 的基本配置方法
- [ ] 能够配置代码分割和优化策略
- [ ] 了解如何集成 React 19 和 TypeScript
- [ ] 掌握环境变量和代理配置
- [ ] 能够配置 PWA 和性能优化
- [ ] 了解测试工具的集成方法

## 🚀 下一步

→ [Webpack 高级配置](./webpack-guide.mdx)  
→ [React 19 构建迁移](./react19-build-migration.mdx)  
→ [CI/CD 流程](../02-cicd/cicd-overview.mdx)

## 🎯 重点提示

### Vite 6.0 新特性
- **React 19 原生支持**: 完全支持 React 19 的新特性和新 JSX Transform
- **性能提升**: 进一步优化启动和构建速度
- **TypeScript 增强**: 改进的类型推断和错误提示
- **插件生态**: 更丰富的插件系统和更好的兼容性

### 配置最佳实践
- **使用别名**: 配置路径别名，提高代码可读性
- **代码分割**: 合理配置代码分割，优化加载性能
- **环境变量**: 使用环境变量管理不同环境的配置
- **缓存策略**: 配置合适的缓存策略，提高构建效率

### React 19 集成要点
- **自动 JSX Transform**: Vite 自动启用新 JSX Transform，无需手动配置
- **类型定义**: 安装对应的 @types/react@19 和 @types/react-dom@19
- **插件更新**: 使用最新版本的 @vitejs/plugin-react
- **构建验证**: 确保构建结果正常工作
